services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: prueba-tecnica-redlabs
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "RedFrog!01"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql

volumes:
  sqlserver-data:
